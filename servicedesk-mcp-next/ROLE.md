# Folder Role

`servicedesk-mcp-next` is the current ServiceDesk live production mirror.

Use this folder for:
- current ServiceDesk runtime
- current ServiceDesk Azure deployment artifacts
- current ServiceDesk validation evidence
- current ServiceDesk live endpoint state

This is the authoritative ServiceDesk connector folder unless and until a later production mirror supersedes it.
