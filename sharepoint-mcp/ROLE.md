# Folder Role

`sharepoint-mcp` is legacy planning and forensic material.

Use this folder only for:
- historical planning context
- rebuild notes that have not yet been absorbed into the canonical baseline
- forensic comparison when needed

Do not treat this folder as the canonical current SharePoint production mirror when `reference-mcp-baseline` already contains the live mirrored SharePoint baseline facts.
