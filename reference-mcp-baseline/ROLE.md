# Folder Role

`reference-mcp-baseline` is the canonical architecture baseline.

Use this folder first for:
- Azure parity
- runtime parity
- connector doctrine
- deployment doctrine
- validation doctrine
- drift control

This folder is authoritative for the shared MCP connector pattern.

For SharePoint specifically, this folder also currently contains the live mirrored Azure facts for the working SharePoint production connector.
