# Folder Role

`connector-template-runtime` is the reusable connector shell.

Use this folder for:
- shared runtime shape
- thin MCP shell reuse
- adapter-only variance
- future connector bootstrapping

Do not treat this folder as a live production mirror.

It is the reusable implementation shell that future target-system connectors should specialize.
